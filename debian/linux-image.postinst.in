#!/bin/sh -e

version=@KERNELRELEASE@
path=/boot/vmlinuz-${version}

case "${1}" in
    (configure)
    ;;
    (*)
        exit 0
    ;;
esac

linux-update-symlinks install ${version} ${path}
linux-run-hooks image postinst ${version} ${path} -- ${@}

#DEBHELPER#
